// Currently DARK only
import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonBackground: {
    padding: 5,
    backgroundColor: '#646464',
    borderWidth: 1,
    borderColor: '#858585',
    borderRadius: 10,
  },
});
